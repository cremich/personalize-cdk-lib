// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for StartTrainingFunction
 */
export interface StartTrainingFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/training/start-training.
 */
export class StartTrainingFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: StartTrainingFunctionProps) {
    super(scope, id, {
      description: 'src/training/start-training.lambda.ts',
      ...props,
      runtime: lambda.Runtime.NODEJS_14_X,
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../assets/training/start-training.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}